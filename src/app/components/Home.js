/**
 * Home view
 * Empty because the main `App` is truly home.
 * This component exists as a route Handler.
 */

import React from 'react';

class Home extends React.Component {
	render() {
		return (
			<div />
		);
	}
};

export default Home;
